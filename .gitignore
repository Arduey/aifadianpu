# ── 环境与密钥 ──
# ⚠️ 根目录 .env 是「占位符模板」,随仓库分发,供新部署 cp 后填值。
# 真实凭据一律写在各服务器的 .env 里(那份填值后请勿再 commit)。
# 其它位置的 .env / *.env 仍全部忽略,避免误提交带密码的副本。
.env
*.env
.env.*
!.env.example
!/.env

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 会话/工具产物 ──
.reasonix/
.tmp_gh/
.tmp_*/
.afd_admin_init.json

# ── 编辑器/系统 ──
.DS_Store
Thumbs.db
.idea/
.vscode/
