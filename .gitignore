# ── 环境与密钥 ──
# 仓库里根目录的 .env 是「空白文件」(0 字节),只为让 clone 后存在该文件;
# 真实凭据一律写在各服务器自己的 .env 里(填值后请勿再 commit)。
# 其它位置的 .env / *.env / .env.* 全部忽略,避免误提交带密码的副本。
.env
*.env
.env.*
!/.env

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── 会话/工具产物 ──
.reasonix/
.tmp_gh/
.tmp_*/
.afd_admin_init.json

# ── 编辑器/系统 ──
.DS_Store
Thumbs.db
.idea/
.vscode/
