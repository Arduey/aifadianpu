# 爱发电铺 · 宝塔面板手把手部署（逐框填写版）

> 全程在**宝塔面板网页**里操作。每个步骤都告诉你：点哪个菜单、出现哪个框、**填什么**。
> 不需要写任何 `.env`，数据库/会话密钥/管理员/SMTP 都在「安装向导」网页里填。

---

> 📚 **文档导航**：[README](README.md) · [全新部署](RE-DEPLOY.md) · [宝塔手把手](BT-PANEL-STEP.md) · [部署教程](DEPLOY-TUTORIAL.md) · [生产功能](PRODUCTION.md)
> 当前页：**宝塔面板手把手（逐框填写版）**（BT-PANEL-STEP.md）

---

## 第 0 步 · 登录宝塔面板

浏览器打开宝塔安装完给的地址（形如 `http://服务器IP:8888/xxxx`），输入用户名密码登录。

---

## 第 1 步 · 装三个软件

左侧菜单 → **软件商店**，分别点「安装」：

1. **Nginx** —— 立即安装
2. **MySQL** —— 选 5.7 或 8.0，安装
3. **Python 项目管理器** —— 安装

> 都装完后他们会显示"已安装"。

---

## 第 2 步 · 建数据库（要给安装向导填的值）

左菜单 → **数据库 → 添加数据库**，右边表单：

| 框 | 填什么 |
|---|---|
| 数据库名 | `afdianpu` |
| 用户名 | 自动生成（或自填，比如 `afdianpu`） |
| 密码 | 点「随机生成」或自己设一个，**复制保存** |
| 访问权限 | 本机即可（默认） |
| 字符集 | `utf8mb4`（下拉选） |

点「确定」。建好后，在这行左侧会看到 库名/用户名/密码 —— **这 5 个值（HOST固定127.0.0.1、PORT固定3306、NAME、USER、PASSWORD）装到安装向导 STEP1 里填。**

> 不要导任何表。表结构程序会自动建。

---

## 第 3 步 · 上传代码

**不要写 .env，直接传 `fastapi/` 内容。**

- 左菜单 → **文件** → 进入 `/www/wwwroot/`
- 点「上传」→ 把本地打包好的 `fastapi` 压缩包传上去 → 右键「解压」→ 重命名为 `afd`
- 最终路径应为：`/www/wwwroot/afd/main.py` 存在即可

> 只上传 `fastapi/` 目录里的东西（main.py、app/、database/、deploy_check.py、requirements.txt…）。

---

## 第 4 步 · 添加 Python 项目（项目管理器怎么填）

左菜单 → **Python 项目管理器 → 添加项目**：

| 表单框 | 怎么填 |
|---|---|
| 项目名称 | `afd`（随意） |
| 项目端口 | `8000` |
| Python 版本 | 下拉选 **3.10+**（如 3.11/3.12） |
| 启动方式 | 下拉只有 `命令行启动 / uwsgi / gunicorn` → **选「命令行启动」** |
| 项目路径 | 点右边「选择」→ 选 `/www/wwwroot/afd` |
| 启动命令 | 填写：`uvicorn main:app --host 127.0.0.1 --port 8000 --workers 2` |
| 环境变量 | 选「无」即可（向导会生成 .env） |
| 安装模块依赖 | ✅ **勾选**（自动 pip install -r requirements.txt） |
| 启动用户 | 默认 `www` |

> 面板没有 `uvicorn` 启动方式，所以用「命令行启动」并在「启动命令」里写 uvicorn。
> ⚠️ **别选 uwsgi**（它不支持 FastAPI 的 asgi）。
> 若要「创建虚拟环境」勾选项，也勾上。

填完点「确定」→ 面板开始建虚拟环境、装依赖 → 装完自动启动。

> 首次启动会自动建数据库表。**没 .env 也能正常启动**（会跳到安装向导）。

---

## 第 5 步 · 项目启动后检查

Python 项目管理器 → 找到 `afd` 项目：

- 「运行状态」应为**绿色/正常运行**；
- 若红色/停止 → 点「日志」看报错，最常见依赖没装完。

---

## 第 6 步 · 绑定域名 + HTTPS

### 方式 A（项目映射，最省事）
Python 项目管理器 → `afd` → 操作 →「**映射**」→ 填你的域名

### 方式 B（反向代理，更可控）
左菜单 → **网站 → 添加站点**：
- 域名：填 `你的域名`
- 根目录：随意（或空）

然后进该站点 → 「反向代理」→ 添加：
- 代理名称：随意
- 目标 URL：`http://127.0.0.1:8000`

### 加 SSL（两方式都做）
网站 → 该站点 → 「**SSL**」→ 申请免费证书（Let's Encrypt）→ 部署 → 开「**强制 HTTPS**」。

> ⚠️ 必须 HTTPS，否则登录 Cookie（Secure）会被浏览器拒绝。

---

## 第 7 步 · 打开网站 → 安装向导

浏览器访问 `https://你的域名`，会**自动进入 `/install`**。

### STEP 1 · 数据库（唯一要手填的框）
| 框 | 填 |
|---|---|
| 数据库主机 | `127.0.0.1` |
| 端口 | `3306` |
| 数据库名 | `afdianpu`（第2步的库名） |
| 数据库用户名 | 第2步的用户名 |
| 数据库密码 | 第2步的密码 |

点「测试并保存」→ 显示"配置已保存，请重启"。

### 重启一次
Python 项目管理器 → `afd` → **重启**。

### STEP 2 · 管理员 + 业务配置
刷新页面（进 `/install`）：
| 框 | 填 |
|---|---|
| 管理员邮箱 | 一个 QQ/163 邮箱 |
| 管理员店铺名 | 中文/字母/数字 ≤12 |
| 管理员密码 | ≥8 位 |
| 确认密码 | 同上 |
| SMTP/APP_BASE_URL/平台 Logo/爱发电账号 | 可选，可留空，后台可再改 |

点「创建管理员并完成」→ 进入「数据预览」= 部署完成。

---

## 第 8 步 · 收尾

- 之后注册的新账号一律是**普通商户**；管理员唯一来自安装向导。
- 管理员在「个人中心 → 平台配置」可改 SMTP/平台账号等。

---

## 常见卡点对照

| 现象 | 原因 / 改法 |
|---|---|
| 「运行状态」红/停止 | 看「日志」；多半依赖没装完，或端口 8000 被占 |
| 网站打开是 502 | 反代目标没对上，或项目没起来（回到第 5 步） |
| 打开还是首页、不进安装向导 | 数据库里 `platform_settings` 有旧 `installed=1`，清空该表后重启 |
| STEP1 连接失败 | MySQL 没启动 / 库名密码填错 / 端口不对 |
| 重启后再进仍是 STEP1 | 没真正重启，回「重启」再试 |
