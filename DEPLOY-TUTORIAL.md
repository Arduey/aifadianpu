# 爱发电铺 · FastAPI 版宝塔部署教程(从零到上线)

> 环境假设:**宝塔 Linux 面板 + Nginx + MySQL**。项目为 **FastAPI + MySQL + Jinja2 模板**(无前端构建),全部操作几乎都在宝塔面板的图形界面里完成,适合不懂命令行的新手。
>
> 本教程专门针对「宝塔 Python 项目管理器」的实际情况编写 —— 面板**没有 uvicorn 启动方式**,只有 `命令行启动 / uwsgi / gunicorn`,下文有明确选项。

---

> 📚 **文档导航**：[README](README.md) · [全新部署](RE-DEPLOY.md) · [宝塔手把手](BT-PANEL-STEP.md) · [部署教程](DEPLOY-TUTORIAL.md) · [生产功能](PRODUCTION.md)
> 当前页：**宝塔部署教程（从零到上线）**（DEPLOY-TUTORIAL.md）

---

## 0. 准备清单

| 需要的东西 | 说明 |
|---|---|
| 一台宝塔面板的云服务器 | 1 核 1G 即可(演示/小规模足够) |
| 一个域名 | 已解析到服务器 IP,**必须能开 HTTPS**(生产 Cookie 用 Secure,HTTP 下登录会被浏览器拒绝) |
| MySQL 数据库存放空间 | 面板自带的 MySQL 即可 |
| FTP/SFTP 工具 | 或用宝塔「文件」在线上传 |

---

## 1. 安装运行环境(约 5 分钟)

打开宝塔面板 → **软件商店**,安装:

- **Nginx**(Web 服务器,负责反代 + HTTPS)
- **MySQL 5.7 或 8.0**(数据库,装完记下 root 或用新建库的账号密码)
- **Python 项目管理器**(宝塔插件,内置 Python 3.10+ 和 pip;项目管理、进程守护、日志全在里面)

> 已经装过的直接跳过。MySQL 只需一个库即可,Python 项目管理器会自动带 Python。

---

## 2. 创建数据库

宝塔面板 → **数据库 → 添加数据库**:

- 数据库名:`afdianpu`(可自定义)
- 用户名 / 密码:自己设一个(记住,**第 4 步写进 .env 要用**)
- 字符集:选 **utf8mb4**

创建完成后,数据表**不用手动建**——项目首次启动会自动创建(幂等)。想看表结构也可用 phpMyAdmin 导入 `database/schema.sql`,二选一即可,推荐让程序自动建。

---

## 3. 上传项目到服务器

把整个 `fastapi/` 目录的内容上传到站点路径,例如 `/www/wwwroot/afdianpu`。

上传后目录结构应类似:

```
/www/wwwroot/afdianpu/
├── main.py
├── requirements.txt
├── .env                   # (重要!)你的私有配置,不要外传
├── database/schema.sql
├── app/
│   ├── config.py          # 读取 .env
│   ├── db.py              # 连接 MySQL
│   ├── auth.py / utils.py / render.py
│   ├── routes_public.py / routes_console.py
│   ├── services/
│   ├── templates/
│   └── static/
```

**上传方式任选**:

- **FTP/SFTP**:宝塔「FTP」里给站点建账号,或用 SSH/SFTP 工具(`FileZilla`、宝塔「文件」在线压缩包上传后解压)。
- **压缩包上传**:本地把 `fastapi` 文件夹打成 zip → 宝塔「文件」上传到 `/www/wwwroot/` → 右键解压 → 改名为 `afdianpu`。

> ⚠️ `.env` 里含数据库密码和会话密钥,**绝不要提交到 Git / 发给别人**。

---

## 4. 配置(全自动,无需改文件)

**你不需要创建或编辑任何文件,包括 `.env`。**

所有配置(数据库连接、会话密钥、SMTP、平台账号、对外域名等)都在**网页安装向导**里填写:

- **数据库连接**在向导「第 1 步」填写 → 系统**当场测试连接**,成功后**自动生成 `.env`**(其中 `SESSION_SECRET` 由程序用 `secrets.token_hex(32)` 随机生成,无需你自己算);
- **会话密钥 / APP_BASE_URL / SMTP / 爱发电消费者账号 / 平台 LOGO** 等在向导「第 2 步」或之后的「个人中心 → 平台配置」里填写,存入数据库,随时可改;
- 项目在**尚未配置时也能正常启动**(建表失败只记日志),访问域名会自动把你引导到 `/install`,所以你可以先启动、后配置。

> 下面这张表**仅供理解各配置项的含义**,实际填写都在网页上完成,不需要手改文件。

| 配置项 | 说明 |
|---|---|
| `DB_HOST` / `DB_PORT` | 数据库地址与端口(本机 `127.0.0.1` / `3306`) |
| `DB_USER` / `DB_PASSWORD` / `DB_NAME` | 数据库账号、密码、库名(如 `afdianpu`) |
| `SESSION_SECRET` | 会话(JWT)签名密钥,**由向导自动生成** |
| `APP_BASE_URL` | 对外访问域名(不带末尾斜杠),用于邮件里的链接 |
| `APP_SECURE` | 生产 = `true`(需 HTTPS);本地 http 调试 = `false` |
| `AFDIAN_LIVE` | 历史遗留开关：**当前业务代码不再读取**（下单/查单固定走真实接口） |
| `ENABLE_PREVIEW_LOGIN` | 生产一定设 `false`(关掉预览通行证后门) |
| `SMTP_HOST/PORT/USER/PASS/FROM` | 发邮件用(QQ/163 用授权码);不配 = 仅写日志 |

**SMTP 说明(发支付通知邮件,可选)**:QQ 邮箱为例 —— 主机 `smtp.qq.com`、端口 `465`、用户名你的 QQ 邮箱、密码填**SMTP 授权码**(在 QQ 邮箱「设置-账户」里开启 SMTP 后生成,**不是登录密码**);这些都在网页「平台配置 → SMTP」里填。

---

## 5. 添加 Python 项目(核心步骤)

宝塔面板 → **Python 项目管理器 → 添加项目**,按下面填:

| 表单项 | 填什么 |
|---|---|
| **项目名称** | 随便起,如 `afdianpu` |
| **项目端口** | `8000`(**和下面启动命令里的端口一致**) |
| **Python 版本** | 3.10+(面板里如 3.12.8) |
| **启动方式** | 👇 看下面「启动方式怎么选」 |
| **项目路径** | `/www/wwwroot/afdianpu` |
| **环境变量** | 选「无」即可(项目读 `.env` 文件) |
| **安装模块依赖** | ✅ 勾上(自动 `pip install -r requirements.txt`)**这是必须的** |
| **启动用户** | 默认 www 即可 |

### ⭐ 启动方式怎么选(重点)

宝塔的「启动方式」**只有 `命令行启动 / uwsgi / gunicorn`,没有 uvicorn**,所以:

- 推荐 **A. 命令行启动**(最简单可靠):
  - **启动命令**填:
    ```
    uvicorn main:app --host 127.0.0.1 --port 8000 --workers 2
    ```
  - `uvicorn` 已在 `requirements.txt` 里,勾了「安装模块依赖」后直接可用。
  - 这时的「入口文件 / 通讯协议 / 应用名称」这些字段**是给 gunicorn/uwsgi 用的,可以空着不管**。

- 或者 **B. gunicorn 启动**:
  - **通讯协议**必须选 **asgi**(⚠️ 千万别选 wsgi,否则 FastAPI 起不来)
  - **启动文件 / 应用**填 `main:app`
  - `gunicorn` 也已在 `requirements.txt` 里。

> 🚫 **不要选 uwsgi**:uwsgi 只支持 wsgi 同步协议,不适合 FastAPI(asgi)。

填完点「确定」→ 面板开始创建虚拟环境、装依赖 → 装完自动启动。首次启动时自动建数据库表。

**常见现象**:
- 加完等十几秒,「运行状态」显示绿色=启动成功。
- 如果红/停,点进项目「日志」看报错,最常见是:`.env` 没建 / MySQL 地址或密码错 / 端口被占。

---

## 6. 绑定域名 + HTTPS

两种方式任选:

### 方式一:项目管理器直接映射域名(最省事)
Python 项目管理器 → 找到项目 → 「操作 → 映射」→ 绑定你的域名,选好站点。

### 方式二:新建站点反向代理(更可控,推荐)
1. **网站 → 添加站点**,域名填你的域名,根目录随意(或留空)。
2. 进该站点 → 「反向代理」→ 添加,目标 URL 填 `http://127.0.0.1:8000`。
3. 到「SSL」给域名**申请免费证书(Let's Encrypt)** → 部署 → 开启「**强制 HTTPS**」。

> ⚠️ `APP_SECURE=true` 时必须走 HTTPS,否则浏览器拒绝写入会话 Cookie,登录会失效。

部署完成后:

```
你的域名 ──HTTPS─▶ Nginx(反代 127.0.0.1:8000) ──▶ FastAPI app
```

---

## 7. 上线初始化(首次登录必做)

打开 `https://你的域名`,会自动跳转 **`/install` 安装向导**(全程网页,无需改文件):

1. **第 1 步:填数据库连接**(主机/端口/库名/账号/密码)——会当场测试连接,成功后自动写入 `.env`(含自动生成的 `SESSION_SECRET`),并提示回项目管理器**点「重启」**。
2. **第 2 步(重启后自动进入):创建管理员账号**(邮箱/店铺名/密码),同时填平台配置(对外域名、爱发电消费者账号、平台 LOGO、SMTP 等)。提交即完成安装。
   > 管理员由向导创建,**不走注册页**;注册页只用于后续普通商户自助开店。
3. 之后进 **个人中心(账户)**:
   - 完善平台配置(爱发电消费者账号、LOGO、微信/支付宝开关)
   - 绑定你作为商户的爱发电 `user_id` / `token`(必填,否则无法创建商品与销售)
4. **商品管理 → 创建商品**,包括创建分类为「API充值」的商品作为商户充值档位。
5. 把店铺链接(按爱发电 user_id 定位)发给买家即可。

**关于下单**:下单与查单都走**真实爱发电接口**(`afd_live.py`),因此需要先在「平台配置」里填好**爱发电(消费者)账号/密码**(平台会用它自动登录并维护凭证)。若尚未配置,下单会返回明确的错误提示而不是模拟成功。

**关于邮件**:未配置 SMTP 时不发信、仅记日志;配好后才会真发支付通知与找回密码邮件。

---

## 8. 日常运维(全图形化)

- **更新代码**:覆盖上传文件 → 项目管理器点「重启」→ 即生效(无前端构建)。
- **改表结构**:更新代码后重启项目,启动时会幂等同步表(建议先备份)。
- **备份**:宝塔「计划任务」→ 每天备份数据库 + 备份项目目录。
- **看日志**:项目管理器「日志」页(回调失败 / 邮件失败可定位原因)。
- **开机自启**:命令行启动方式下,项目管理器默认托管进程,无需额外配置;若手动跑,用「进程守护」(supervisor)托管。

---

## 9. 常见问题排查

| 症状 | 原因 / 解决 |
|---|---|
| 页面一直转圈 / 502 | 服务没起来:项目管理器看运行状态;或端口(8000)被占,换端口要同步改反代目标 |
| 登录不上 / 没有保持登录 | 访问走了 HTTP,不是 HTTPS → 强制 HTTPS |
| 数据库报错 connection refused | `DB_HOST` 别填外网 IP,本机用 `127.0.0.1`;确认 MySQL 已启动 |
| 首次启动报 Table 不存在 | 说明没自动建表:确认 `.env` 的 MySQL 账号有建表权限;或手动导入 `database/schema.sql` |
| 收不到邮件 | 检查 `SMTP_*`;QQ/163 要用**授权码**而非登录密码;看项目管理器日志 |

---

## 附:纯命令行的等效手搭方式(不用项目管理器)

```bash
cd /www/wwwroot/afdianpu

python3 -m venv venv
./venv/bin/pip install -r requirements.txt
./venv/bin/uvicorn main:app --host 127.0.0.1 --port 8000 --workers 2
# 用「进程守护」(supervisor)托管上面这条命令即可开机自启
# 首次访问会自动进 /install 向导,配置由网页填写并自动写入 .env(无需 cp/编辑)
```
