# 爱发电铺 · 生产功能说明

> 本文描述 fastapi 版**当前已实现并部署**的生产行为,供开发者/运营快速对齐「系统到底怎么工作」。

---

> 📚 **文档导航**：[README](README.md) · [全新部署](RE-DEPLOY.md) · [宝塔手把手](BT-PANEL-STEP.md) · [部署教程](DEPLOY-TUTORIAL.md) · [生产功能](PRODUCTION.md)
> 当前页：**生产功能说明**（PRODUCTION.md）
> 接口细节见站内「开放接口」页（`/api-docs`）。

---

## 1. 安装向导(首次初始化)

- 首次访问自动进入 `/install`,分两步,**全部网页填写,无需手工创建/编辑任何文件**:
  - **STEP1 数据库**:填 `DB_HOST / 端口 / 库名 / 用户名 / 密码` → **当场测试连接** → 成功后写入项目 `.env`(`SESSION_SECRET` 由 `secrets.token_hex(32)` 自动生成) → 提示回面板**点「重启」**;
  - **重启后 STEP2**:创建**管理员**(邮箱/店铺名/密码),可同时填对外域名、爱发电消费者账号、平台 LOGO、SMTP 等 → 置 `installed` → 进入控制台。
- **管理员唯一来源 = 安装向导**;此后注册的一律为普通商户。
- 未配置数据库时项目**仍能正常启动**(建表失败只记日志),以便你进入向导。

## 2. 注册(`POST /`)

- 字段:邮箱、**邮箱验证码**、店铺名、密码、确认密码。
- **邮箱验证码**:填邮箱 →「获取验证码」(1 分钟重发限制);**未配置 SMTP 时明确失败**(不伪造演示码);提交时校验,错误在表单内提示,不整页刷新。
- **邮箱域名限制**:由管理员在「平台配置 → 允许的邮箱域名」中设置(分号/逗号分隔);**为空则不限制任何域名**。前台提示与校验文案会跟随该设置变化,不再硬编码 `qq.com / 163.com`。
- 店铺名:汉字按 2 字符、其他按 1 字符,合计 ≤12(纯汉字 ≤6),禁特殊符号,实时提示。
- 密码:8~20 位,实时校验;确认密码须一致。
- 提交为 AJAX,失败不丢已填内容;成功**自动登录**并跳转数据预览。
- 输入框统一禁用历史记录(`autocomplete="off"`)。

## 3. 登录(`POST /login`)

- 可用**邮箱或店铺名** + 密码。
- **失败锁定按 IP**:同一来源 IP 5 分钟内连续 5 次失败 → 锁该 IP 5 分钟(经 `X-Forwarded-For` 取真实 IP)。
- Tab 只在输入框间切换。
- **预览通行证**:`?preview=admin|merchant` 仍存在(`main.py` 中间件 + `auth.py`),用于免密预览;**由 `ENABLE_PREVIEW_LOGIN` 控制,生产必须为 `false`**(默认 `true`,仅方便本地调试)。

## 4. 个人中心(`/console/account`)

- **店铺信息**:店铺名(同注册规则、实时权重)、**头像/Logo**(Logo 即头像,圆形预览)、**店铺介绍**(最多 **3 行**且 ≤60 字符,超行自动截断,实时计数)、店铺名/头像改动保存。
- **爱发电绑定(商户)**:填 `user_id` / `token`,保存时**自动真实请求爱发电校验**;已配置商户每次进入个人中心也会自动复测。徽标三态:**绿「已通过签名校验」**(平台返回 `ec=200`,仅表示能连通并读取,不代表业务级授权终态)/ **红「校验未通过」**(悬停可见原始返回)/ **橙「暂无法校验」**。无独立「测试连接」按钮。
- **通知**:
  - **Webhook**:URL + 方法(`POST`/`GET`)+ 编码 + 请求头 + 查询参数 + 正文模板,并有「**发送测试**」;
  - **邮件**分两组 —— **订单邮件**、**API 余额不足提醒**,每组均有「**预览模板**」(本地即时渲染,不发信)与「**发送测试**」(发自身邮箱;普通商户每日 1 次,管理员不限)。余额提醒另有**阈值**(默认 ¥1,可改)与 **30 分钟防打扰**(同一商户 30 分钟内只发一次)。
- **修改密码**:当前 / 新(8~20)/ 确认(一致)三框,**过滤中文**(`noCn`)、实时校验。
- **管理员「平台配置」**:
  - 爱发电(消费者)账号/密码窄输入 + **「登录测试」**按钮 —— 真实 POST `https://afdian.com/api/passport/login`,把原始 JSON 展示在页面,并把 `auth_token` **持久化写库**(`platform_settings.consumer_auth_token` / `consumer_token_at`);**下单/查单时会自动使用并自动续登**(过期自动重新登录,无需人工点击);
  - SMTP(主机/端口/授权码/From)、付款方式开关与禁用说明、平台域名、平台 LOGO、允许的邮箱域名、单次 API 费用。
  - 迁移提示:升级到本版前须对 `platform_settings` 执行 `ADD consumer_auth_token / consumer_token_at`,否则该页查询报 Unknown column。

## 5. API 费用(`/console/fees`)

- 每笔支付成功扣**服务费**(金额由管理台 `api_fee_cents` 统一配置,当前为 ¥0.25;未配置时回退 `.env` 的 `FEE_PER_ORDER_CENTS`)并写流水(`order_fee`)。**注册赠送 = 该服务费金额**(注册即送一笔,恰好覆盖首单)。
- 入账分类:充值(`recharge`)、注册赠送(`signup_grant`)、管理员调整/退费(`admin_adjust`)。
- 展示:当前余额、累计入账(充值/赠送/退费)、累计支出(服务费),逐笔流水含原因/说明/变动后余额。
- **余额不足**:低于单次服务费时**拦截下单**并邮件提醒商户充值;低于**商户自设阈值**时仅提醒(不拦单)。

## 6. 订单管理(`/console/orders` / 管理员 `admin-orders`)

- 表列:**订单号、下单时间、支付时间、分类(带分类图标,无图标回退平台 LOGO)、商品标题、SKU、渠道、金额、服务费**;管理员额外**商家**列。(留言列已移除)
- 服务费列:已付款显示 `-¥x`,未付款显示 `0`(悬浮提示「待付款未扣费」),两者外观一致。
- 支持关键词搜索、渠道筛选;管理员订单页支持**按商户筛选**。
- **过期未付单自动清理**:订单由「待支付」起,**超过 3 小时仍未支付会被物理删除**(打开订单页、下单、收到回调时都会顺带触发一次惰性清理)。因此**列表只反映现存订单**;「创建订单数」由独立的 `order_stats` 表累计,不受清理影响。

## 7. 数据预览(`/console/dashboard` / 管理员 `admin-stats`)

- 指标:KPI、**创建订单数**、已付款笔数与金额、日均、daily 趋势、商品销量排行。
- **创建订单数口径**:统计现存订单 + `order_stats` 累计**(取较大值)**,故已过期的未付单**仍计入**,不会因清理而变小。

## 8. 商品管理(`/console/products`)

- 顶部加载时提示「请勿创建违法商品,轻则删除商品,重则关闭账户」(数秒后消失)。
- **按分类分组为可折叠卡片**(折叠后只显示分类名与图标,折叠状态本地记忆);分类支持 **↑/↓ 调整顺序**,该顺序**实时同步到前台店铺分类目录**。
- **同一分类共用一个图标**:任一 SKU 编辑时修改图标即**同步该分类所有商品**;未设置图标时前台/订单处**回退平台 LOGO**。
- **「API充值」分类仅管理员可创建**:管理员在该分类下建的商品自动成为**充值项**(不进前台店铺);普通商户建同名分类只当普通商品处理。
- 字段规则:分类 ≤16 字、标题 ≤24 字、SKU ≤12 字、价格 **≥5 元正整数**(5 元为爱发电「自选发电」最低限额);**分类/标题/SKU 均禁 `&`**;同店铺内「分类+标题+SKU」完全一致才判重复;排序改在分类层级。

## 9. Webhook 回调(商户自定义)

- 方法:`POST` 或 `GET`;
- **编码方式(前端可选)**:`JSON` / `form-urlencoded` / `文件(二进制)` / `RAW 原始文本`;(后端另兼容 `field` = `{"payload": "<json字符串>"}` 的旧写法)
- **正文模板**支持 `@变量` 占位,可用:`@order_id`、`@creat_time`、`@type`、`@title`、`@sku`、`@total`、`@seller`;请求头与查询参数同样支持变量替换;
- 支持「**发送测试**」验证送达;每次投递结果记入「**Webhook 历史**」(每商户最多保留 30 条,含测试记录)。
- 触发时机:**订单支付成功时推送一次**(耗时与失败不影响主流程)。

## 10. 产品导览 + 新手任务清单

- **导览**:右上角「导览」按钮可随时重播;首次登录商户自动触发(localStorage 一次性),逐步高亮主菜单各项;**导览按钮仅对非管理员商户显示**(`{% if me.role != 'admin' %}`),管理员不显示。
- **任务清单**(`/console/tasks`):侧栏/顶栏入口;新手任务可勾选、显示进度、完成状态存本地。

## 11. 第三方对接的真实状态(重要)

- **下单 / 查单:已接通真实接口**。走 `app/services/afd_live.py`(`/shop/buy`、充值下单、支付轮询都调用它),真实请求 `https://ifdian.net/api/order/create-order` 与 `https://ifdian.net/api/order/check`,携带消费者 `auth_token`,并用 `curl_cffi` 伪装浏览器指纹**绕过 Cloudflare 1010 拦截**(未安装 `curl_cffi` 时自动回退 urllib,但可能被拦)。
- **登录凭证:已自动维护**。`afd_login.consumer_ensure_token()` 会复用未过期 token,过期则用平台配置的账密自动重新登录并落库;`live_create_auto` / `live_check_auto` 在首次调用失败且疑似登录态失效时,**会强制重登再试一次**。
- **商户绑定校验:已真实请求**爱发电(`afdian.test_connection`,`ec=200` 判定能连通并读取)。
- ✅ **`app/services/afdian.py` 已清理**:原 `create_order` / `order_paid` 占位实现(无调用方)已删除,该文件现只保留只读的 `test_connection()`。
- 可由安装向导 / 平台配置覆盖的业务项(留空则用 `.env` 兜底):`SMTP_*`、`APP_BASE_URL`、`ENABLE_PREVIEW_LOGIN`(`AFDIAN_LIVE` 已无人读取,保留仅为兼容旧配置)。
- 邮件需管理员在「平台配置」配好 SMTP 才真发;未配则仅记日志。

## 环境变量(.env)职责

- **必须由 `.env` 提供**(运维级,网页不可改):`DB_*`、`SESSION_SECRET`、`APP_SECURE`。
- 可由安装向导 / 平台配置覆盖的业务项(留空则用 `.env` 兜底):`SMTP_*`、`APP_BASE_URL`、`AFDIAN_LIVE`、`ENABLE_PREVIEW_LOGIN`。
- `.env` 由安装向导自动生成,**不要手动编辑**。
