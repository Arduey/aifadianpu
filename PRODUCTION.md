# 爱发电铺 · 生产功能说明(与当前实现一致)

> 本文描述 fastapi 版**当前已实现并部署**的生产行为,供开发者/运营快速对齐"系统到底怎么工作"。
> 部署步骤见 `RE-DEPLOY.md` / `BT-PANEL-STEP.md`;接口细节见「开放接口」页(`/api-docs`)。

---

## 1. 安装向导(首次初始化)

- 首次访问会自动进入 `/install`,分两步,全部网页填写、无预填 env:
  - **STEP1 数据库**:填 `DB_HOST/端口/库名/用户名/密码` → 测试连接 → 写入项目 `.env`(会话密钥自动生成)→ 提示重启;
  - 重启后在 **STEP2** 创建**管理员**(邮箱/店铺名/密码)+ 可选 SMTP 等 → 置 `installed` → 进入控制台。
- **管理员唯一来源 = 安装向导**;此后注册的一律为普通商户。

## 2. 注册(`POST /`)

- 字段:邮箱、**邮箱验证码**、店铺名、密码、确认密码。
- **邮箱验证码**:填邮箱→「获取验证码」(1 分钟重发限制,10 分钟有效);**未配置 SMTP 时不产生、明确失败**(无演示伪造)。提交时校验验证码,错误在表单内提示,不刷新页面。
- 邮箱仅 `qq.com / 163.com`,实时校验,不合法不可提交。
- 店铺名:汉字按 2 字符、字母/数字/其他按 1 字符,合计 ≤12(纯汉字≤6),禁特殊符号,实时提示。
- 密码:8~20 位,实时校验;确认密码须一致;输入框**过滤中文**,右侧小眼睛显隐。
- 提交为 AJAX,失败不丢已填内容;成功**自动登录**并跳转数据预览。
- 输入框统一禁用历史记录(`autocomplete="off"`)。

## 3. 登录(`POST /login`)

- 二维码/邮箱或店铺名 + 密码。
- **失败锁定按 IP**:同一来源 IP 5 分钟内连续 5 次失败 → 锁该 IP 5 分钟(经 `X-Forwarded-For` 取真实 IP)。
- 密码框小眼睛显隐;Tab 只在输入框间切换。
- **无"一键体验控制台"**、无 `?preview=` 免密登录(`/auth/demo` 已删除)。

## 4. 个人中心(`/console/account`)

- **店铺信息**:店铺名(同注册规则、实时权重)、**头像/Logo**(Logo 即头像,圆形预览)、店铺介绍(≤60 字,实时计数)、店铺名/头像改动保存。
- **爱发电绑定(商户)**:填 `user_id/token` 保存时**自动真实请求爱发电**校验；已配置商户每次进入个人中心也会自动再测。右上徽标 = 绿“已通过签名校验”(平台返回 `ec=200`,仅表示能连通并读取,不代表业务级授权终态)/ 红“校验未通过”(悬停可见原因) / 橙“暂无法校验”。不再有独立“测试连接”按钮。
- **通知**:Webhook(URL + 方法 POST/GET + 编码 raw/form/field + **发送测试**)。邮件分两组 —— **订单邮件**、**API 余额不足提醒**,每组均有“预览模板”(本地即时渲染不发信)与“发送测试”(发自身邮箱,普通商户版每日 1 次/管理员不限;余额提醒测试/预览用当前真实余额);余额提醒另有阈值(默认 ¥1)与 30 分钟防打扰。
- **修改密码**:当前/新(8~20)/确认(一致)三框,小眼睛、过滤中文、实时校验。
- **管理员「平台配置」**:账号/密码窄输入后带 **“登录测试”** 按钮 —— 真实 POST 爱发电 `/api/passport/login`,把返回原始 JSON 展示在页面,并将 `auth_token` **持久化写库**(字段 `consumer_auth_token`,`consumer_token_at`),供代下单等流程复用;凭证随会话失效时,用已保存账密自动重新登录获取。其余:SMPT 等请接入逻辑。
  - 迁移提示:升级到本版前须对 `platform_settings` 执行 `ADD consumer_auth_token / consumer_token_at`(详见更新/部署步骤),否则该页查询报 Unknown column。

## 5. API 费用(`/console/fees`)

- 每笔支付成功扣 ¥0.20 服务费并写流水(`order_fee`)。
- 入账分类:充值(`recharge`)、注册赠送(`signup_grant`)、管理员调整/退费(`admin_adjust`)。
- 展示:当前余额、累计入账(充值/赠送/退费)、累计支出(服务费),逐笔流水含原因/说明/变动后余额。

## 6. 订单管理(`/console/orders` / 管理员 `admin-orders`)

- 表列:订单号、支付时间、分类、商品标题、SKU、渠道、金额、留言、**服务费**;管理员额外**商家**列。
- 支持关键词搜索、渠道筛选;管理员订单页支持**按商户筛选**。

## 7. 商品管理(`/console/products`)

- 顶部加载时提示「请勿创建违法商品,轻则删除商品,重则关闭账户」(5 秒后消失)。
- 创建/编辑/删除;分类「API充值」自动成为充值商品(不进前台店铺)。

## 8. Webhook 回调(商户自定义)

- `POST` 或 `GET`;
- 编码方式:
  - `raw`：直接把 JSON 作为 POST 请求体;
  - `form`：表单字段 `payload=<urlencoded JSON>`;
  - `field`：POST 一个 JSON `{"payload": "<json字符串>"}`;
  - `GET`：追加 `?payload=<urlencoded JSON>`。
- 支持「发送测试」验证送达。

## 9. 产品导览 + 新手任务清单

- **导览**:右上角「导览」按钮可随时重播;首次登录商户自动触发(浏览器 localStorage 一次性),逐步高亮主菜单各项。
- **任务清单**(`/console/tasks`):侧栏/顶栏入口;6 项新手任务可勾选、进度显示、完成状态存本地;任务项可配置。

## 10. 当前仍为演示/未接入的部分

- **爱发电真实收款(下单)** — 仍为 `AFDIAN_LIVE=false`(演示/本地模拟,订单约 12 秒自动成功);真实款另需按爱发电文档接通 create-order(见「待接入」)。
- **商户绑定鉴权 / 消费者登录凭证** — 已真连(见上);但商户“服务真实下单(pipeline 走真实爱发电 open-api 的下单+收款确认)”仍未接入。
- 邮件需要管理员在「平台配置」配好 SMTP 才真发;未配则为日志演示。

## 环境变量(.env)职责

- **必须由 .env 提供**（运维级,网页不可改）:`DB_*`、`SESSION_SECRET`、`APP_SECURE`。
- 可由安装向导/平台配置覆盖的业务项(留空则用 .env 兜底):`SMTP_*`、`APP_BASE_URL`、`AFDIAN_LIVE`、`ENABLE_PREVIEW_LOGIN`。
